# 来都来了 · 使用说明（保姆级）

> 给麦冬看的。按编号一步步来，每一步都标了「谁做」和「要多久」。

---

## 一、这到底是个啥

一句话：给你用的**执业医备考小台子**——打卡、看大纲进度、刷真题、跟 AI 医学角色聊天、被出题考、顺便被问一句"今天动了吗"。

一共 6 个页面（左边或上面一排按钮）：

1. **今日** — 点「我来了」打卡 + 笔试倒计时 + 大纲进度 + 每日一句先贤寄语
2. **大纲** — 2025 版执业医大纲 1025 个细目，点一下记"过了几轮"
3. **刷题** — 7284 道执业医考题，点选项立刻判对错、看解析，错题可以重刷
4. **日历** — 热力图，看这个月来了几天、连了几天
5. **陪伴** — **AI 在这儿**：7 位医学角色陪你聊、考你
6. **设置** — 填考试日期、换主题、导出备份

---

## 二、先在本地打开看看（2 分钟，强烈建议先做）

> **别去搜网址。这个台子现在还没上线，网上任何 `study-ai.xxx` 跟我无关。**
> 它就在你电脑这个文件夹里：
> `D:\todo代码写长一点不然真的很难找\工作台 wb转codex版\study-ai\index.html`

**⚠️ 千万别直接双击 `index.html`** —— 那样打开会显示「大纲加载失败」。原因是浏览器有个安全规矩（`file://` 下禁止读取本地文件），必须走一个小服务才行。

**正确做法（一步到位）：双击 `study-ai` 文件夹里的 `双击启动.bat`**

| 现象 | 说明 |
| --- | --- |
| 弹出一个黑框命令行窗口 | ✅ 正常，**别关它**，关了网页就打不开了 |
| 浏览器自动打开 `http://127.0.0.1:8765/index.html` | ✅ 成功，开撸 |
| 黑框里写「Python not found」 | ❌ 没装 Python，让我来修 |

黑框里按 `Ctrl + C`、或直接关掉窗口 = 停止本地服务（关了就没了，想再看再双击一次）。

**本地能干啥 / 不能干啥**：
- ✅ 能：看界面、点打卡、勾大纲细目、看日历热力图、换主题 —— 数据存在浏览器本地
- ❌ 不能：跟 AI 聊天（没部署，AI 接口不存在）
- ⚠️ 注意：本地记的进度**不会**同步到以后上线的版本，属于"试用"，看清布局就行

---

## 三、第一次：把它弄上线（只做一次，20–30 分钟）

> **为啥非要上线？** 因为数据在云端、AI 在网上。
> 本地双击打开也能看界面、点着玩，但 **AI 连不上、数据不同步**。要真正用起来，得先上线。

**准备**：你的智谱 API Key（就是海贼王运动台用的那个，在 open.bigmodel.cn 拿）

**✅ 第 0 步已完成**：仓库建好了 → https://github.com/yidaoyi/study-ai （你选了 **public**），代码已全部推上去（14 个文件）。

> 顺带说一句：仓库 public + 站点没做登录，意味着**知道网址的人都能打开**（里面只有打卡和进度，没有隐私信息）。
> 以后想收紧，两条路：把仓库转 Private（Netlify 照样能拉），或让我加个简单密码门。

| 步骤 | 谁做 | 多久 | 怎么做 |
| --- | --- | --- | --- |
| 1 | ~~AI~~ **已完成** | — | 代码已在 https://github.com/yidaoyi/study-ai |
| 2 | 你 | 3 分 | 打开 https://app.netlify.com → **Add new site → Import an existing project** → 选 **GitHub** → 授权后选 `yidaoyi/study-ai` |
| 3 | 你 | 1 分 | 构建设置**别改**（它会自动读 `netlify.toml`：build command = `npm install`，publish = `.`）→ 点 **Deploy** |
| 4 | 你 | 2 分 | 站点建好后：**Site configuration → Environment variables → Add a variable** → 名 `ZHIPU_API_KEY`，值 = 你的智谱 key（open.bigmodel.cn 拿，跟海贼王台子同一个就行）<br>**页面上那些选项全都别动**（见下方「环境变量那一页怎么填」） |
| 5 | 你 | 3 分 | （可选）再加 `WECOM_WEBHOOK_URL` = 企微群机器人地址 —— 想在手机上收每日提醒就配，**拿法见下方** |
| 6 | 等 | 2 分 | **改完环境变量要手动重新 Deploy 一次**（Deploys → Trigger deploy → Deploy site），否则 key 不生效 |
| 7 | 你 | 2 分 | 打开站点网址（Netlify 给的 `xxx.netlify.app`，可在 Site configuration → 改名为 `zhiyiban`）→ 先点「我来了」试试 |
| 8 | 你 | 2 分 | 手机用 **Safari / Chrome / Edge**（别用微信内置浏览器）打开网址 → "添加到主屏幕"，就有 App 图标了 |

> 💡 **想省事**：Netlify 支持直接用 GitHub 账号登录，全程点鼠标，不用填信用卡。

### 企微群机器人地址怎么拿（想收每日提醒才需要，嫌麻烦可直接跳过）

**手机端（企业微信 App，最省事）**：

1. 企业微信 → 底部「消息」→ 右上角 **⊕** → **发起群聊** → 拉 **1 个人**进来（同事、小号都行；拿到地址后可以把人移出去，地址照样有效）
2. 进群 → 点右上角 **「···」** → 往下滑找 **群机器人** → **添加机器人**
3. 名字填「来都来了」→ 创建
4. 点一下刚建好的机器人 → 复制 **Webhook 地址**
   形如：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=693a91f6-xxxx-xxxx-xxxx-xxxxxxxxxxxx`
5. 整条粘进 Netlify 的 `WECOM_WEBHOOK_URL` → Create variable → **再 Trigger deploy 一次**

**电脑端**：群聊窗口 → 右上角群名旁的 **「···」** → 群机器人 → 添加机器人 → 复制 Webhook。

> 不想收提醒就**整步跳过**，打卡 / AI / 大纲都不受影响，只是没人早上喊你。
> 这个地址属于半私密（拿到就能往你群里发消息），别贴到公开地方。

---

### 环境变量那一页怎么填（页面上选项很多，其实只填两个空）

Netlify 新版这个页面花里胡哨，**除了「Key」和「Value」，其他一律保持原样别碰**：

| 页面上的东西 | 怎么办 |
| --- | --- |
| **Key** | 填 `ZHIPU_API_KEY` |
| **Value / Secret** | 粘贴你的智谱 key（形如 `xxxxxxxx.yyyyyyyy`，在 open.bigmodel.cn 的 API Keys 页面复制） |
| Contains secret values（开关） | **不管**，开不开都能用 |
| Scopes（All / Specific，写着 Upgrade to unlock） | **不管**，保持默认 All scopes（Specific 要付费，别理） |
| Values（Same value for all / Different value） | **不管**，保持默认「Same value for all deploy contexts」 |
| 下面 Project policies / Sensitive variable policy | **完全不用管**，那是给团队协作防泄密的，咱们单人不涉及 |

填完点绿色 **Create variable**。

**⚠️ 最最容易漏的一步**：因为站点在你配 key **之前**就构建过一次，Netlify **不会**自动用新 key 重新构建。
必须手动再来一次：**Deploys 页 → 右上角 Trigger deploy → Deploy site**。不点这个，AI 不会回话。

### ⚠️ 卡住高发点：搜不到仓库（`No repositories found`）

Netlify 之前只为**海贼王那个仓库**授权过，新仓库它看不见。解决办法：

1. 就在这个页面上点蓝色的 **「Configure Netlify on GitHub」**
2. 跳到 GitHub → 可能要输入 GitHub 密码确认 → 找到 **Repository access** 区块
3. 选 **Only select repositories** → 下拉里勾 **`study-ai`** → 点 **Save**（绿色按钮）
4. **回到 Netlify 那个页面**，按 `F5` 刷新 → 再搜 `study`，仓库就出来了

> 如果 GitHub 页面没反应 / 找不到那个按钮：直接开 https://github.com/settings/installations → 点 **Netlify** 右边的 **Configure** → 同样勾 `study-ai` → Save。

**验收 3 条（全过才算成功）**：
- 点「我来了」→ 变成「今天来过啦 ✓」
- 去「陪伴」→ 跟张仲景说句话 → 他回你了
- 大纲页随便点个圆圈 → 数字变成 1

---

## 四、日常怎么用（每个动作 ≤10 秒）

- **打卡**：进页面就点大圆钮「**来都来了**」。按下去有一声"叮铃"（星露谷那种味儿，本地合成的，不联网）。
  想安静就在圆钮右边点「🔔 音效：开」关掉，它会记住。**漏打不归零**，别有心理负担。
- **记进度**：大纲页 → 找到今天看的那条 → 点圆圈。点一下 = 1 轮，再点 = 2 轮（越熟轮次越多）。鼠标移到行尾有「−」减一轮、「×」清零。
- **看坚持**：日历页，深色格子 = 来了的日子。
- **被考**：陪伴页 → 选个角色 → 点「考考我」。TA 会挑你**还没掌握**的细目出题；你打字回答，TA 点评对错、补知识点。再点「结束出题」退出。
- **随便聊**：陪伴页直接打字。TA **看得到你的真实进度**，也会顺口问你今天动没动（运动数据来自你的海贼王运动台）。
- **倒计时**：设置页填目标考试日期，首页就出现倒计时。
- **刷题**：见下面第五节。

---

## 五、刷题怎么用（7284 道题，手机最好用）

左边点 **✎ 刷题**。第一次进会加载题库（3MB，Wi-Fi 下几秒，之后浏览器会缓存）。

**1. 选个范围** —— 六个筛选项，都不选就是全部 7284 题：

| 筛选项 | 怎么选 |
|---|---|
| 类别 | **执业医师**（你考的这个）/ 助理医师 |
| 来源 | **历年真题** / 模拟题 |
| 卷组 | 第 1–5 卷（数据集内部编号，**不是真实年份**，题库里压根没年份信息） |
| 题型 | 基础概念 / 理论题 / 分析诊断 |
| 顺序 | 顺序 / 乱序（想打乱就选乱序） |
| 只刷 | 全部 / **我的错题** / **没做过的** |

选完底下会显示「选中 X 题 · 已做 Y 题，其中错 Z 题」，点**开始刷题**。

**2. 答题** —— 点选项就判：

- **答对** → 绿框，0.9 秒后**自动进下一题**
- **答错** → 你选的那个**标红**、正确答案**标绿**，下面出解析，**不自动跳**，停在那儿让你看完，自己点「下一题 ›」
- 答过的题再点第二下不改判（防止手滑改答案）

**3. 重刷**（2026-09-19 改的重点）—— 以前答过的题再进来是锁死的，没法重做。现在：

- 答过的题底下会显示「**↻ 重做本题**」和「做过 N 次 · 累计错 N 次 · 上次选 B」
- 点「重做本题」→ 选项解锁，可以重新选
- 重做**不会清掉历史**：每次作答都记进账本，错题本按**最近一次**判定（重做答对了就移出错题，但"累计错几次"仍留着）

**4. 错题本** —— 退出到选题区，把「只刷」改成**我的错题**，就只剩你最近一次做错过的题。

答题记录**跟打卡一样上云**，换手机也在。

### 伤寒专科（郝万山讲题）

左边 **🎙️ 伤寒专科** 是单独的一册：**从 7284 题里筛出 471 道《伤寒论》相关的题**。

- 想让郝万山讲题，先在这页把「讲题按钮」打开（默认关，省着用）
- 之后刷到伤寒相关的题，答完会出现「🎙️ 让郝万山讲讲这道题」，他会引着讲稿给你讲
- **为什么不是所有题都能讲**：讲稿是《伤寒论》讲课实录，西医诊断、妇儿、针灸、法规这些他不讲。
  硬让他讲只会变成 AI 套着他的口吻瞎编，所以宁可不亮按钮。这是刻意的，不是没做完。

### 两句关于题库的实话

- 题是真的执业医考题（A1/A2 病例题、方剂题都有），但**不是完整版历年真题**，是公开能拿到的一个子集。
- **大约一半的解析不完整**（末尾戛然而止），这类我会标「（这条解析可能不完整）」。少数题根本没解析。
- 数据集本身有 OCR 错字（缺字、标点乱码）。**拿来练手感可以，别拿它当标准答案背**，有疑问回头翻教材。

---

## 六、7 位 AI 角色（想换人设，跟我说一声就行）

- 📜 **张仲景**（医圣）：严厉慈祖，甩原文考你、背不出不许歇
- 🌿 **李时珍**（药圣）：温柔博学，讲采药故事、用草木比喻
- ⚕️ **华佗**（神医）：既考医理也催你动 —— "不动则朽"
- 🍵 **孙思邈**（药王）：慢条斯理，医德 + 养生
- 🌸 **小苓**（实习学妹）：跟你一起刷题的战友，会共情、会一起骂题难
- 🎓 **导师**（现代博导）：问你课题/论文进展，先肯定再指正
- 🎙️ **郝万山**（伤寒大家）：口语化，先讲临床故事再落条文，强调"抓主证"

### 不满意某人的人设？自己写一版（角色私设）

7 个角色**都能**改，不用找我。设置页 →「角色私设」：

1. 点上面的角色名，选你要改的那位
2. 在框里写你自己的设定（想写多细都行，上限 2000 字）
3. 勾「启用私设」→ 点**保存私设**
4. 不想用了：取消勾选保存，或点**恢复官方人设**

**你写的这段会替换掉什么**：性格、说话方式、TA 跟你的关系、经典台词。
**不会被替换的**：【回复规则】和【防脱题指令】——系统会自动接在你写的后面，跟其他角色一字不差。
所以你把小苓改成再奇怪的性格，她也不会突然开始用 markdown、自称 AI。

写的时候注意：
- 最好第一句就写「你是 XXX，……」，把身份交代清楚
- 想改称呼就直接写："称呼我麦冬，不许叫我宝"
- 生效后角色卡片上会标 **✎私设**，方便你记住哪几个被改过

私设跟打卡数据存在一起上云，**换手机打开专属链接还在**。

---

## 七、发给别人玩：数据怎么隔离（重要，先看）

**结论：直接把网址原样发出去就行，安全。**

数据是按「身份码」分开存的，像一个个带锁的抽屉：

| 打开方式 | 进哪个抽屉 |
|---|---|
| 你打开**自己的专属链接**（网址末尾带 `?u=你的身份码`） | 你的抽屉 |
| 朋友打开**原样网址**（不带 `?u=`） | 他**第一次**打开时自动分配一个**全新的空抽屉** |
| 你换手机/换浏览器 | 打开专属链接 → 还是你的抽屉 |

所以：
- 朋友看不到你的进度，也改不了你的数据
- AI 跟朋友聊的时候**不会叫他"麦冬"**，也不会提你的课题组、运动记录（访客模式）
- 你自己的身份码，在**设置 → 我的身份**里能看到、能复制专属链接、能改

⚠️ **别把"专属链接"发给别人** —— 那等于把你的抽屉钥匙给他了。分享给别人用**不带 `?u=` 的干净网址**。

### 分享出去，会花你的 AI 额度

朋友每跟 AI 聊一句，都走你的智谱 key。已经上了**两道闸**：

| 闸 | 上限 | 说明 |
|---|---|---|
| 每人每天 | 40 条 | 按身份码算（清缓存会刷新，所以单靠它不够） |
| 全站访客合计 | 400 条/天 | **真正的防线**，超了当天访客都聊不了，你自己不受影响 |

10 个人以内随便用，烧不了几个钱（`glm-4-flash` 很便宜）。真被人刷爆，改 `chat.js` 里那两个常量就行。

### 朋友打不开怎么办

**2026-09-19 实测：Netlify 在国内是能打开的**，只是可能慢一点。先让朋友点一下试试。

真打不开，按麻烦程度从低到高：

1. **换个网络**（Wi-Fi 换流量，或反过来）—— 一半情况这样就解决了
2. **发离线包**：把 `index.html` + `app.js` + `outline.json` + `data/` 打个 zip 发给朋友，
   解压后双击 `index.html` 就能刷题（**AI 聊天用不了**，其他功能都在）
3. **绑自己的域名 + 备案** → 走国内节点，速度最快，但要 20–30 天，分享给几个人不值得

⚠️ 别为了"国内更快"去迁平台 —— 不备案的话，腾讯云 EdgeOne 也是走境外节点，速度没本质差别，
还要把 4 个后端函数重写一遍（Blobs 换 KV、cron 要另找），不划算。

---

## 八、常见问题

**Q：网上搜到 `study-ai.me` / `study-ai.com`，是我的站吗？**
一律**不是**。我**唯一**的仓库是 https://github.com/yidaoyi/study-ai （账号 `yidaoyi`），网上同名站点是别人的产品，别去注册、别输账号密码。
真正上线后，网址只会出现在下面第三节（部署）和你自己的 Netlify 后台 —— **其他地方看到的都不算数**。

**Q：AI 不回话？**
先确认已经部署、且配了 `ZHIPU_API_KEY`。本地直接双击打开网页时 AI 用不了（这是正常的）。

**Q：换手机/电脑，进度还在吗？**
在 —— **前提是用你的专属链接打开**（设置 → 我的身份 → 复制专属链接）。
在新设备上打开这个链接一次，身份码就记住了，以后直接开网址也行。
⚠️ 如果在新设备上**直接开裸网址**，会被当新访客、分到空抽屉，看起来像"进度没了"—— 别慌，用专属链接打开就回来了。

**Q：数据会丢吗？**
云端一般挺稳，但建议你**每月**「设置 → 导出备份」存一份 JSON 到网盘或 U 盘。真丢了用「导入恢复」。

**Q：别人能看到我的进度吗？**
不能。每个人一个抽屉（见第六节）。除非你把**专属链接**发出去了 —— 那就等于把钥匙给他了。
另外提醒：网址本身是公开的、没做登录，所以知道网址的人都能**用**（只是用他自己的空抽屉）。

**Q：AI 会跑题/乱说吗？**
人设里写了「防脱题指令」：保持角色、不懂就说角色台词不装懂、禁"根据资料显示"这种机械腔。发现 TA 出戏，跟我说"XX 跑题了"，我改一下人设文件就好。

**Q：智谱要花钱吗？**
`glm-4-flash` 很便宜，你自己用基本等于不要钱。分享给别人也烧不了多少 —— 已经加了**两道闸**（每人 40 条/天 + 全站访客合计 400 条/天），超了当天访客聊不了，你不受影响。发给熟人放心发，别往公开论坛发就行（那是题库版权的问题，不是钱的问题）。

**Q：大纲是最新的吗？**
是 2025 版执业医大纲（1025 个细目 = 理论 842 + 技能 183）。以后换新版要换 `outline.json`，跟我说一声。

**Q：这 7284 道题哪来的？会不会有错？**
来自公开的开源数据集 `Bolin97/TCMLE`（Apache-2.0 授权，允许使用）。题目是真考题，但**不保证全对**：大约一半的解析被截断、数据集有 OCR 缺字。拿来练手感很好，**遇到跟教材冲突的，以教材为准**。

**Q：能按科目（比如只刷方剂学）刷题吗？**
暂时不能。现在只能按 类别 / 真题或模拟 / 年份 / 题型 筛。想做科目分类的话跟我说，可以做，但要给题库打标签，我得先解决"方剂学"这类关键词误判的问题。

**Q：刷题记录会丢吗？**
跟打卡数据一起上云，换设备在。但注意：**如果题库重新生成导致题号变了，旧的答题记录会对不上号**。正常不会去动题库，你不用担心。

---

## 九、几条提醒

- 没有账号密码，靠「身份码」区分人 —— **你的专属链接就是钥匙，收好**
- 想换身份码：设置 → 我的身份 → 输入框填新的 → 切换身份（会用新抽屉，旧数据还在旧码下）
- 每天 07:00 / 13:00 会有提醒推到企微（前提是你配了 `WECOM_WEBHOOK_URL`）
- 每次改功能，我会让 AI 在 `HANDOFF_AI.md` 末尾记一条版本记录 —— **以后接手的人（或下一个 AI）先看那个文件**
